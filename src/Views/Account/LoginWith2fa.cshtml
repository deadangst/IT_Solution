@model LoginWith2faViewModel
@{
    ViewData["Title"] = "Autenticación de dos factores";
}

<h2>@ViewData["Title"]</h2>
<hr />
<p>Tu inicio de sesión está protegido con una aplicación autenticadora. Ingresa tu código de autenticación a continuación.</p>
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@ViewData["ReturnUrl"]">
            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TwoFactorCode"></label>
                <input asp-for="TwoFactorCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="TwoFactorCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="checkbox">
                    <label asp-for="RememberMachine">
                        <input asp-for="RememberMachine" />
                        @Html.DisplayNameFor(m => m.RememberMachine)
                    </label>
                </div>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-default">Iniciar sesión</button>
            </div>
        </form>
    </div>
</div>
<p>
    ¿No tienes acceso a tu dispositivo autenticador? Puedes
    <a asp-action="LoginWithRecoveryCode" asp-route-returnUrl="@ViewData["ReturnUrl"]">iniciar sesión con un código de recuperación</a>.
</p>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
