@{
    ViewData["Title"] = "Desactivar la autenticación de dos factores (2FA)";
    ViewData.AddActivePage(ManageNavPages.TwoFactorAuthentication);
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Esta acción solo desactiva el 2FA.</strong>
    </p>
    <p>
        Desactivar el 2FA no cambia las claves utilizadas en aplicaciones autenticadoras. Si deseas cambiar la clave
        utilizada en una aplicación autenticadora, deberías <a asp-action="ResetAuthenticatorWarning">
            restablecer tus
            claves de autenticador.
        </a>
    </p>
</div>

<div>
    <form asp-action="Disable2fa" method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Desactivar 2FA</button>
    </form>
</div>
