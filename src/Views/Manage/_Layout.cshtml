@{
    Layout = "/Views/Shared/_Layout.cshtml";
}

<h2>Gestiona tu cuenta</h2>

<div>
    <h4>Cambia los ajustes de tu cuenta</h4>
    <hr />
    <div class="row">
        <div class="col-md-3">
            @await Html.PartialAsync("_ManageNav")
        </div>
        <div class="col-md-9">
            @RenderBody()
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", required: false)
}
