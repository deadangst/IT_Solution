@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Se produjo un error mientras se procesaba su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de la Solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de Desarrollo</h3>
<p>
    Cambiar al entorno <strong>Desarrollo</strong> mostrará información más detallada sobre el error que ocurrió.
</p>
<p>
    <strong>El entorno de desarrollo no debe estar habilitado en aplicaciones desplegadas</strong>, ya que puede resultar en que la información sensible de las excepciones se muestre a los usuarios finales. Para la depuración local, el entorno de desarrollo puede habilitarse configurando la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> a <strong>Desarrollo</strong>, y reiniciando la aplicación.
</p>
